CREATE TABLE [SAMPLE].[SAMPLE_AUDIT_DATA]
(
  [Id] BIGINT NOT NULL IDENTITY(1,1) PRIMARY KEY,
  [IdAudit] BIGINT NOT NULL FOREIGN KEY REFERENCES [SAMPLE].[SAMPLE],
  [IdType] INT NOT NULL FOREIGN KEY REFERENCES [DICT].[AUDIT_DATA_TYPES],
  [JSON] VARCHAR(255)
)
